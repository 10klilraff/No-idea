;----------------------------------------------
;misc functions for webp-resource change branch
;----------------------------------------------
@K52_SELLER_CHECK()
#FUNCTION
#DIM CONST CHARA = 52
;[MODIFIED CONTENT] Reisen's medicine seller attire patch, when working or in the village, or if set is on (see if just checking for the set is enough?)
;currently disabled set tracking as she tends to be in those clothings for majority of the time, or being at the village (work only) ;|| INRANGE(CFLAG:ARG:現在位置, 200, 299) ;
SIF (CFLAG:CHARA:職種 == 41 && BASE:CHARA:仕事量 > 0); || CFLAG:CHARA:基本服装セット == OBJNAME_TO_ID(CHARA, "GET", "衣装セット", "うどん薬売りセット")
	RETURNF 1
SIF CHK_DATENOW(CFLAG:CHARA:デート中) && CFLAG:CHARA:同行 && CHK_DATENOW(CFLAG:MASTER:デート中) == 3 && !CHECK_NUDE(CHARA)
	RETURNF 1
	