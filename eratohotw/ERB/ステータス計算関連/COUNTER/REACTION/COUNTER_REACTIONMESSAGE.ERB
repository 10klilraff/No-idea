;膣性交
@EVENT_COUNTE_MESSAGE500
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW %CALLNAME:TARGET%'s folds coil around %PARSE("your")% %TINKO(PLAYER)%, clinging to it as if seeking %PARSE("your",1)% %SAMEN_DESCRIPTION(PLAYER)%...
	;だいしゅきホールド
	CASE 2
		PRINTFORMW %CALLNAME:TARGET% embraces %PARSE("you")% while wrapping %HIS_HER(TARGET)% legs around them...
	;性交見せつけ
	CASE 3
		PRINTFORMW %CALLNAME:TARGET% bends back, showing off where %PARSE("your")% %TINKO(PLAYER)% enters %HIM_HER(TARGET)%...
	CASE 4
		PRINTFORMW %CALLNAME:TARGET% womb descends, wanting %PARSE("your")% %SAMEN_DESCRIPTION(PLAYER)%...
	CASE 5
		PRINTFORMW %CALLNAME:TARGET% thrusts %HIS_HER(TARGET)% hips against %PARSE("you")% unconsciously, driving %PARSE("your",1)% %TINKO(PLAYER)% deeper inside...
	CASE 6
		PRINTFORMW The mouth of %CALLNAME:TARGET%'s womb opens and closes, waiting impatiently for %PARSE("your")% ejaculation...
	CASE 7
		PRINTFORMW %CALLNAME:TARGET% entwines %HIS_HER(TARGET)% fingers with %PARSE("yours",1)%...
ENDSELECT
RETURN 1
;アナル性交
@EVENT_COUNTE_MESSAGE501
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW The walls of %CALLNAME:TARGET%'s hot, slimy insides coil around %PARSE("your")% %TINKO(PLAYER)%...
	CASE 2
		PRINTFORMW %CALLNAME:TARGET% entwines %HIS_HER(TARGET)% fingers with %PARSE("yours",1)%...
ENDSELECT
RETURN 1
;手淫
@EVENT_COUNTE_MESSAGE502
;現状許可を貰うには童貞喪失必須
;IF NOWEX:PLAYER:射精 && TALENT:PLAYER:非童貞 <= 0 && !TALENT:無知 && CFLAG:生解禁 == 2
IF NOWEX:PLAYER:射精 && !TALENT:無知 && CFLAG:生解禁 == 2
	PRINTFORML %CALLNAME:TARGET% holds %PARSE("your")% %TINKO(PLAYER)% against %HIS_HER(TARGET)% vagina as %HE_SHE(TARGET)% picks up the pace...
	
ELSEIF NOWEX:PLAYER:射精
	PRINTFORML %CALLNAME:TARGET% jerks %PARSE("your")% %TINKO(PLAYER)% faster and faster as %PARSE("you",1)% %PARSE("approach")% climax...
ELSE
	PRINTFORML %CALLNAME:TARGET% holds %PARSE("your")% %TINKO(PLAYER)% and moves up and down slowly...
ENDIF
RETURN 1

;フェラチオ
@EVENT_COUNTE_MESSAGE503
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW %CALLNAME:TARGET% holds the tip of %PARSE("your")% %TINKO(PLAYER)% in %HIS_HER(TARGET)% mouth as %HE_SHE(TARGET)% strokes it gently...
	CASE 2
		PRINTFORMW %CALLNAME:TARGET%'s tongue crawl along not just %PARSE("your")% balls, but %PARSE("your",1)% %TINKO(PLAYER)% as well, servicing %PARSE("you",5)% diligently...
	;ディープスロート
	CASE 3
		PRINTFORMW %CALLNAME:TARGET% swallows %PARSE("your")% %TINKO(PLAYER)% to the base.
		PRINTFORMW %HIS_HER(TARGET,1)% tongue and throat thoroughly caressed the entire length...
	CASE 4
		PRINTFORMW %CALLNAME:TARGET% continues sucking %PARSE("your")% %TINKO(PLAYER)% as %HE_SHE(TARGET)% touched %HIM_HER(TARGET)%self...
ENDSELECT
RETURN 1

;パイズリ
@EVENT_COUNTE_MESSAGE504
SELECTCASE TCVAR:21
	CASE 1
		IF TALENT:TARGET:バストサイズ > 0
			PRINTFORMW %CALLNAME:TARGET% holds %PARSE("your")% %TINKO(PLAYER)% between %HIS_HER(TARGET)% breasts...
		ELSE
			PRINTFORMW %CALLNAME:TARGET% rubs %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)% against the head of %PARSE("your")% %TINKO(PLAYER)%...
		ENDIF
	;縦パイズリ,乳ファック
	CASE 2
		PRINTFORMW %PARSE("You thrust")% %PARSE("your",1)% %TINKO(PLAYER)% into %CALLNAME:TARGET%'s plump cleavage...
	;谷間を作る
	CASE 3
		IF TALENT:TARGET:バストサイズ < 0
			PRINTFORMW %CALLNAME:TARGET% manages to create something like a valley with %HIS_HER(TARGET)% chest and tries to massage the %TINKO(PLAYER)% with it...
		ELSE
			PRINTFORMW %CALLNAME:TARGET% makes a valley by holding %HIS_HER(TARGET)% breasts together, and moves %HIS_HER(TARGET)% whole upper body up and down, massaging %PARSE("your")% %TINKO(PLAYER)%...
		ENDIF
	;パイズリフェラ
	CASE 4
		PRINTFORMW %CALLNAME:TARGET% has %PARSE("your")% %TINKO(PLAYER)% trapped between %HIS_HER(TARGET)% breasts as %HE_SHE(TARGET)% licks the tip poking out...
ENDSELECT
RETURN 1

;素股
@EVENT_COUNTE_MESSAGE505
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW %CALLNAME:TARGET% has %HIS_HER(TARGET)% thighs wrapped around %PARSE("your")% %TINKO(PLAYER)% as %HE_SHE(TARGET)% moves %HIS_HER(TARGET)% %HIP(TARGET)% back and forth...
	;手淫追加
	CASE 2
		PRINTFORMW %CALLNAME:TARGET% has %HIS_HER(TARGET)% thighs wrapped around %PARSE("your")% %TINKO(PLAYER)% as %HE_SHE(TARGET)% moves %HIS_HER(TARGET)% %HIP(TARGET)% back and forth while caressing the tip with %HIS_HER(TARGET)% fingers...
ENDSELECT
RETURN 1

;無理矢理膣性交
@EVENT_COUNTE_MESSAGE506
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW %CALLNAME:TARGET% twists and turns, trying to get away from %PARSE("you")%...
	CASE 2
		PRINTFORMW %CALLNAME:TARGET% stares at %PARSE("you")% with tears in %HIS_HER(TARGET)% eyes...
ENDSELECT
RETURN 1


;乳首責め
;Commented out in COUNTER_REACTION.ERB for some reason
@EVENT_COUNTE_MESSAGE510
PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の乳首
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW を指でつまみ、クリクリと刺激した…
	CASE 2
		PRINTFORMW に舌を這わせた…
ENDSELECT

RETURN 1

;玉揉み
@EVENT_COUNTE_MESSAGE600
IF TCVAR:21 == 1
	IF SOURCE:加虐
		PRINTFORMW %CALLNAME:TARGET%は%CALLNAME:PLAYER%の陰嚢に手を伸ばし揉みほぐしてきた…
	ELSE
		PRINTFORMW %CALLNAME:TARGET%は%CALLNAME:PLAYER%の陰嚢に手を伸ばし優しく揉みほぐしてきた…
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:TARGET%は陰嚢を咥えて口の中でコロコロと玉を転がしている
ENDIF
RETURN 1

;アナル愛撫
@EVENT_COUNTE_MESSAGE601
IF ABL:PLAYER:Ａ感覚 < 2
	PRINTFORMW %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアヌスを指で刺激している…
ELSE
	PRINTFORMW %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアヌスに指を挿入し直腸までかき回した…
ENDIF
RETURN 1